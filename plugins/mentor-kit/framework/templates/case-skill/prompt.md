# <在此写任务描述>

描述一个会**触发目标 skill** 的真实任务，内联必要的真实素材（代码片段、文件内容、报错信息等）。

要点：
- **不要在 prompt 里点名要用的 skill** —— 让被测 agent 自然决定是否调用，
  这样「是否触发 skill」才能成为有效信号（配合 `baseline:true` 对比）。
- 内联真实素材，而不是让 agent 去猜；任务边界要清楚。
