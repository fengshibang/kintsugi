# War3 MCP Server - Python Dependencies
# ======================================
# HTTP 结果回传服务（server/http_receiver.py）依赖 Flask：
#   pip install -r requirements.txt
# 缺 Flask/werkzeug 时 8766 端口不监听，所有 test_commit 会因无进度上报而静默超时。
# Python 版本要求：3.7+

flask>=2.0
werkzeug>=2.0

# 其余功能仅使用 Python 标准库（已内置，无需安装）：
# asyncio / json / socket / struct / subprocess / threading
# signal / logging / pathlib / datetime / platform / typing
